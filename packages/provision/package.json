{
  "name": "@maker/provision",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Maker first-run provisioning: hardware detection, curated model catalog, integrity, and the offline gate.",
  "license": "MIT",
  "main": "src/index.ts",
  "dependencies": {
    "@maker/engine": "*"
  },
  "scripts": {
    "test": "node --test \"test/**/*.test.ts\""
  }
}
